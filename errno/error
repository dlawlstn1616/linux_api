거의 대부분의 시스템 호출과 라이브러리 함수는 어떤 형태로든 성공 여부를 나타내는 상태값을 리턴한다. 

# 시스템 호출 에러 처리

특정 값이 에러를 나태내지만 보통 -1을 리턴한다. 

#include <stdio.h>

void perror(const char *msg);

#include <string.h>

char *strerror(int errnum);

# 라이브러리 함수의 에러 처리

